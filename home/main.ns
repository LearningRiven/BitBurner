import {rootAllHelper} from "/automation/rootNodes.ns";
import {killAllHelper} from "/automation/stopProcesses.ns";
import {moveHelper} from "/automation/moveFiles.ns";
import {fileRunner} from "/automation/runFiles.ns";

export async function main(ns) {
	let p1 = await rootAllHelper(ns);
    let p2 = await moveHelper(ns);
    Promise.all([p1,p2]).then(() => {
        killAllHelper(ns);
        fileRunner(ns);
    });
}